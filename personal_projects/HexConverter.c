#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <ctype.h>
#include <string.h>

/**
* RGB_to_Hex - aims to convert RGB Value to Hexcode
* @R: the numeric Red value
* @G: the numeric Green value
* @B: the numeric Blue value
*
* Description: This function aims to perform an operation on
* the provided RGB values to convert them to a Hexcode equivilent
*
* Return: The Hexcode
*/

unsigned int RGB_to_Hex(int *R, int *G, int *B)
{
	int arr[3];

	unsigned int hexcode;


	return (hexcode);
}
