#include "functions.h"
#include <string.h>

char *reverse_string(char *string)
{
    int length, left, right;
    char tmp;

    length = (int)strlen(string);
    left = 0;
    right = length - 1;

    while(left <= right)
    {
        tmp
    }
}